<h1>Order History</h1>

<% if @orders.any? %>
  <table>
    <thead>
      <tr>
        <th>Order Number</th>
        <th>Customer Name</th>
        <th>Shipping Address</th>
        <th>Phone Number</th>
        <th>Payment Method</th>
        <th>Total Amount</th>
        <th>Order Date</th>
      </tr>
    </thead>
    <tbody>
      <% @orders.each do |order| %>
        <tr>
          <td><%= order.id %></td>
          <td><%= order.customer_name %></td>
          <td><%= order.shipping_address %></td>
          <td><%= order.phone_number %></td>
          <td><%= order.payment_method %></td>
          <td><%= number_to_currency(order.bill) %></td>
          <td><%= order.created_at.strftime('%b %d, %Y') %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
<% else %>
  <p>No orders found.</p>
<% end %>